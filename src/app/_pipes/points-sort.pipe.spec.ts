import { PointsSortPipe } from './points-sort.pipe';

describe('PointsSortPipe', () => {
  it('create an instance', () => {
    const pipe = new PointsSortPipe();
    expect(pipe).toBeTruthy();
  });
});
